#!/usr/bin/env bash
# OBSOLETO: esto desplegaba a Vercel. Desde 2026-09-21 el sistema real corre en el VPS.
# Usa ./deploy.sh chollos desde la raíz del repo (ver README, sección "Flujo VPS").
set -euo pipefail
echo "Este script ya no se usa — Vercel quedó inactivo desde la migración al VPS."
echo "Corre esto en su lugar:"
echo
echo "  ./deploy.sh chollos"
exit 1
